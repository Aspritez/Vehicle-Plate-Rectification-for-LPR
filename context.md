# 📋 บันทึกประวัติและสรุปการดำเนินงานโครงการ (Project Context & Progress Log)

โครงการ: **ระบบตรวจจับและปรับระนาบป้ายทะเบียนรถยนต์จากกล้องวงจรปิด (CCTV License Plate Detection & Geometric Homography Rectification)**

---

## 📌 สรุปภาพรวมและเป้าหมายของโครงการ (Project Overview)
พัฒนาโซลูชันระบบปัญญาประดิษฐ์เพื่อตรวจจับป้ายทะเบียนรถยนต์จากกล้องวงจรปิด (CCTV) ที่มีมุมกล้องเอียง หลากหลายทิศทาง และนำภาพป้ายทะเบียนที่ถูกตัด (Crop) มาดึงระนาบกลับมาเป็นภาพหน้าตรงแนวนอน 100% ตามทฤษฎีเรขาคณิตการแปลงพิกัด (Geometric Homography Transformation) เพื่อเตรียมพร้อมสำหรับระบบอ่านตัวอักษรป้ายทะเบียน (OCR)

---

## 🛠️ รายละเอียดงานและฟังก์ชันทั้งหมดที่ได้ดำเนินการไปแล้ว

### 1. การจัดการชุดข้อมูลและรูปแบบเฉลย (Dataset & Annotation Management)
- **รูปแบบการทำเฉลย (Annotation Format):**
  - กำหนดการวาดโพลิกอน 4 จุดมุมขอบป้ายทะเบียน (4-Point Quadrilateral Polygon) ผ่านโปรแกรม **AnyLabeling** บันทึกเป็นไฟล์ `.json` คู่กับไฟล์ภาพในโฟลเดอร์ `data/raw_images/`
- **ระบบแปลงข้อมูลและการแบ่งชุดข้อมูล (`split_dataset.py`):**
  - พัฒนาสคริปต์แปลงพิกัดจาก AnyLabeling `.json` ไปเป็นฟอร์แมต **YOLO-OBB (Oriented Bounding Box)** ในรูปแบบ `class_id x1 y1 x2 y2 x3 y3 x4 y4`
  - ทำการสุ่มแบ่งชุดข้อมูล Train / Val ตามอัตราส่วนมาตรฐาน พร้อมสร้างไฟล์คอนฟิก `data.yaml`

---

### 2. การเทรนโมเดล AI ตรวจจับป้ายทะเบียนแบบหมุนเอียง (YOLO-OBB Training Pipeline)
- **สคริปต์การเทรน (`train_obb.py`):**
  - สร้างสคริปต์สำหรับเทรนโมเดล YOLO OBB โดยใช้สถาปัตยกรรม Ultralytics YOLOv8-OBB
  - บันทึกและคัดเลือกน้ำหนักโมเดลที่ดีที่สุดไว้ที่ `best.pt`
  - รองรับการเรียกกดเทรนโมเดลใหม่ผ่าน GUI ได้โดยตรง

---

### 3. การพัฒนา GUI ทดสอบระบบ (`test_gui.py`)
พัฒนาโปรแกรมหน้าต่างกราฟิก (Desktop GUI) แบบ **Modern Dark Theme (Cyberpunk/Neon Aesthetic)** ด้วย Python Tkinter + Pillow + OpenCV:
- **โครงสร้างการแสดงผล 2 ฝั่ง (Side-by-Side View):**
  - **ฝั่งซ้าย:** แสดงภาพต้นฉบับจากกล้อง CCTV พร้อมวาดกรอบ OBB นีออนสีเขียว (AI) หรือสี่เหลี่ยมคางหมูสี Cyan (เฉลย JSON), จุดมุม 4 จุด, ค่า Confidence, และความเร็ว Inference
  - **ฝั่งขวา:** แสดงการเปรียบเทียบแบบคู่ (Side-by-Side):
    1. **Raw Crop:** ภาพตัดตามมุมกล้องเดิมที่เอียง
    2. **Homography Rectified:** ภาพที่ผ่านการดึงระนาบกลับเป็นหน้าตรง 340x150 px
- **เครื่องมือควบคุมบน Toolbar:**
  - 📂 ปุ่มเปิดไฟล์รูปภาพภายนอก
  - 🎲 ปุ่มสุ่มภาพจากคลัง CCTV
  - ◀ / ▶ ปุ่มเปลี่ยนรูปถัดไป / ย้อนกลับ
  - 🤖 ดรอปดาวน์เลือกโมเดล (รองรับหลายเวอร์ชัน เช่น `best.pt`, `runs/...`)
  - 📐 ดรอปดาวน์เลือกโหมดทฤษฎีเรขาคณิต (Auto, 8-DOF, 6-DOF, 4-DOF, 3-DOF)
  - 🎯 เช็คบ็อกซ์สลับโหมด: **`🎯 ใช้เฉลย (.JSON) ถ้ามี`** (เปิดใช้งานเป็นค่าเริ่มต้น)
  - 🏷️ ปุ่มลัด **`🏷️ รูปที่มีเฉลย`** สำหรับกระโดดข้ามไปยังรูปถัดไปที่มีไฟล์ `.json` ทันที
  - 🎚️ แถบเลื่อนปรับค่า Confidence Threshold (10% - 95%)
  - 💾 ปุ่มบันทึกภาพป้ายทะเบียนที่ผ่านการปรับระนาบแล้วทั้งหมดลงในโฟลเดอร์ `predictions/cropped_plates/`

---

### 4. ทฤษฎีเรขาคณิตและการดึงภาพกลับตรง (Geometric DOF Homography)
สร้างฟังก์ชัน `analyze_dof_and_rectify` สำหรับวิเคราะห์เวกเตอร์ของเส้นขอบป้ายทะเบียนและเลือกการแปลงพิกัดตามระดับ Degree of Freedom (DOF):

| ระดับ DOF | รูปแบบการแปลง (Transformation) | พารามิเตอร์ที่คงที่ / เงื่อนไขที่ใช้ | วัตถุประสงค์ในการใช้งาน |
| :--- | :--- | :--- | :--- |
| **8-DOF** | **Projective (Homography)** | อัตราส่วนไขว้ (Cross-ratio) คงที่, มีการลู่เข้าหา Vanishing Point | **แก้ภาพ 3D Perspective Foreshortening ที่ถ่ายจากมุมเฉียงด้านข้างรุนแรง ให้กลับมาราบตรง 100%** |
| **6-DOF** | **Affine Transformation** | เส้นขนานคงที่ (Parallelism Preserved), มีแรงเฉือน (Shear) | แก้ไขภาพที่มีการบิดเบี้ยวจากมุมกล้องระดับปานกลาง |
| **4-DOF** | **Similarity Transformation** | เส้นขนานและมุมฉาก 90° คงที่ (Rotation + Translation + Uniform Scale) | หมุนและปรับขนาดโดยรักษาสัดส่วนด้านกว้าง:ยาวเดิม |
| **3-DOF** | **Euclidean / Rigid** | รูปร่าง มุม และขนาดคงที่ 100% (Scale = 1.0) | หมุนและเลื่อนตำแหน่งอย่างเดียว |

---

### 5. การแก้ปัญหาจุดมุมและการเอียงในมุมมองรุนแรง (Slope-Based Ordering & Exact Homography)
1. **การเรียงลำดับจุดมุม 4 จุด (`order_plate_corners_physical`):**
   - แก้ปัญหาเคสที่ป้ายทะเบียนถูกบีบ Perspective อย่างรุนแรงจนความกว้างพิกเซลสั้นกว่าความสูง (เช่น `Car433.jpg`) โดยใช้มุมความชันของเส้น (`get_line_angle`) แยกแยะขอบบน-ล่าง (< 45°) และขอบซ้าย-ขวาออกจากกันได้อย่างแม่นยำ
2. **การแก้ไขปัญหาภาพเอียงและมุมแหว่ง (Golden Benchmark Match):**
   - เชื่อมต่อพิกัด 4 จุดจริงจากไฟล์เฉลย Ground Truth `.json` เข้าสู่กระบวนการทำ 8-DOF Projective Homography โดยตรง
   - ตัดขั้นตอน Contour Approx เดิมที่เคยคำนวณผิดพลาดจนตัดมุมบนซ้ายแหว่งออก
   - ผลลัพธ์ใน GUI ปัจจุบันมีความคมชัด แบนราบ ตัวหนังสือและเส้นคั่นอยู่ในแนวนอน 100% ตรงตามไฟล์ต้นแบบ [`hough_perfect_warp.jpg`](file:///c:/CCTV%20-%20Detection/predictions/hough_perfect_warp.jpg)
3. **การปรับระนาบแนวนอนอัตโนมัติ (Hough Line Text Deskew):**
   - ในกรณีตรวจจับด้วย AI OBB ทั่วไป ได้เพิ่มระบบตรวจสอบมุมเอียงของตัวหนังสือด้วย Hough Transform และหมุนปรับระดับให้อยู่ในแนวราบอัตโนมัติ

---

## 📁 โครงสร้างไฟล์สำคัญในโปรเจกต์ (Key Project Files)

```text
c:/CCTV - Detection/
│
├── best.pt                       # โมเดล YOLO-OBB ล่าสุดสำหรับตรวจจับป้ายทะเบียน
├── train_obb.py                  # สคริปต์ตั้งค่าและสั่งเทรนโมเดล YOLO-OBB
├── split_dataset.py              # สคริปต์แปลง JSON เป็น YOLO-OBB และแบ่ง Train/Val
├── test_gui.py                   # โปรแกรม GUI หลักสำหรับทดสอบและดึงระนาบ Homography
├── test_detect.py                # สคริปต์ทดสอบการตรวจจับและ Homography ผ่าน Command-line
├── context.md                    # บันทึกประวัติและสรุปการดำเนินงานโครงการ (ไฟล์นี้)
│
├── data/
│   ├── raw_images/               # โฟลเดอร์เก็บภาพดิบจาก CCTV (*.jpg, *.png) และไฟล์เฉลย (*.json)
│   └── dataset/                  # โฟลเดอร์ชุดข้อมูลสำหรับเทรน (images/labels: train & val)
│
└── predictions/                  # โฟลเดอร์เก็บผลลัพธ์การทดสอบ
    ├── cropped_plates/           # โฟลเดอร์บันทึกภาพป้ายทะเบียนที่ดึงตรงแล้ว
    ├── hough_perfect_warp.jpg    # ภาพต้นแบบที่ดึงตรง 100%
    └── final_gui_live_output.jpg # ภาพผลลัพธ์ล่าสุดจากระบบ GUI
```

---

*บันทึกข้อมูลล่าสุด ณ วันที่ 18 กันยายน 2026*
