# Local-development dependencies.
# Inherits everything from requirements.txt and replaces the headless OpenCV
# variant with the full build, which includes optional GUI helpers useful
# for debugging (cv2.imshow, etc.).
#
# NOTE: opencv-python and opencv-python-headless conflict — pip will uninstall
# the headless package and install the full one. This is intentional.
#
# Usage:
#   pip install -r requirements-local.txt
#   streamlit run app.py
#
# Or simply run the provided launcher:
#   .\run_local.ps1

streamlit>=1.34
streamlit-image-coordinates==0.4.1
opencv-python>=4.10.0
numpy>=1.26,<3
Pillow>=10.0.0
easyocr>=1.7.2
